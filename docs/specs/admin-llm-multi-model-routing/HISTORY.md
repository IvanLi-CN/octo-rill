# 演进记录（管理员 LLM 多模型路由与故障切换）

- 2026-06-28: 创建 follow-up spec，冻结“同 provider 多模型 + 顺序优先 + 连续 3 次最终失败后冷却 10 分钟 + 冷却到期自动恢复”的合同。
- 2026-06-28: 完成后端 runtime model routing、管理员多模型设置 UI、预算口径收敛、状态接口扩展，以及 Storybook / Playwright / Rust 校验与视觉证据落盘。
- 2026-08-15: 冻结最近 50 个 UTC 小时的逐模型终态活动合同，采用独立只读接口、默认活动图与组件内卡片切换；沿用 `llm_calls` 与七日保留期，不新增 migration 或 ADR。
- 2026-08-15: 移动端活动图改为紧凑视图，默认隐藏模型名并使用水平起止时间；接口继续保留完整 50 小时。
- 2026-08-16: 活动图改为按容器可用宽度动态展示最近窗口（最多 50 小时）；移除横向滚动与模型名开关，窄屏以优先级标记和下方图例保持紧凑。
- 2026-08-16: 列聚合浮窗改为 pointer-following body portal，在 12px 安全间距内自动避开网格和视口边界；浮窗安全区消除进入浮窗时的悬浮闪退。
- 2026-08-16: 稀疏时间刻度改为水平边界内对齐；末桶替代距离不足三格的常规刻度，消除首尾截断与右侧重叠。
- 2026-08-16: 浮窗与活动网格增加 `min(30vh, 12rem)` 高度上限和纵向滚动，覆盖矮视口与多模型场景。
- 2026-08-16: 调用列表新增精确模型和终态时间范围筛选，活动图单元格与模型状态卡可通过右键、键盘菜单或显式操作入口跳转至可分享的失败/全部调用结果；跳转重置冲突筛选并聚焦结果区域。
- 2026-08-16: 调用时间筛选收敛为单一范围控件；时间口径、起止边界和结束时间排他上限在同一交互面中呈现。
