# 接口契约（Contracts）

本目录记录 `sak35-release-binary-assets` 的发布接口契约。

- `cli.md`：维护者手动触发 `Release` workflow 的 dispatch/backfill 入口。
- `file-formats.md`：GitHub Release 公开资产 `octo-rill-linux-x86_64.tar.gz` 的目录结构。
