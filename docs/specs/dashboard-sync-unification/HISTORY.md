# 演进记录（Dashboard 同步入口收敛与顺序固定）

## 生命周期

- Lifecycle: active
- Created: 2026-03-27
- Last: 2026-07-03

## 变更记录

- 2026-03-27: 创建规格，冻结“单一同步入口 + 顺序固定 + Storybook 视觉证据”的执行口径。
- 2026-03-27: 完成 Dashboard 同步入口、空态文案与 reaction 提示收敛，并更新 Dashboard Storybook stories。
- 2026-03-27: 通过 `bun run lint`、`bun run build`、`bun run storybook:build`，并补入 Storybook 视觉证据，状态更新为 `部分完成（2/3）`。
- 2026-03-27: 根据最新产品口径移除顶部 `Refresh`，保持头部只剩 `同步` 与 `Logout`，并完成视觉证据刷新。
- 2026-03-27: 将最终保留的视觉证据收敛为一张默认态截图，移除多余截图资产。
- 2026-03-27: 创建 PR #39 并完成快车道收敛，规格状态更新为 `已完成`。
- 2026-03-27: 根据 PR review proof，将全量同步改为顺序等待 SSE 完成后再刷新页面，并在后续步骤失败时仍刷新已完成部分；若刷新也失败，则显式上报刷新错误，避免页面停留旧数据却被静默吞掉。
- 2026-04-09: 为 Dashboard Header 与次级控制区补充显式分组语义，并新增“唯一同步入口”Storybook/E2E 回归，防止 `同步` 文案误落到中间控制区。
- 2026-07-03: 同步进度气泡从纯同步状态驱动扩展为可临时 dismissed 的浮层：点击页面空白处或按 `Escape` 关闭当前气泡，但不影响后台同步；同步结束后重置本地关闭状态，下一次同步仍自动显示进度。
- 2026-08-09: 修复同步进度气泡收起后无法恢复的问题：hover、聚焦或点击同步按钮均可重新显示当前详情；重复点击不再发出替代详情的 toast，也不会创建第二个同步任务。
- 2026-08-09: 加固受控 Tooltip 的恢复路径：同步中由 Radix `onOpenChange`、`pointerenter`、`pointermove` 和 focus 共同恢复详情，避免单一 hover 事件失效时再次丢失进度气泡。
- 2026-09-23: 冻结下一阶段同步体验需求：同步开始时详情默认收起；按钮背景以 `task.progress` 确认阶段为锚点，使用不越界、单调、连续的预测进度和低干扰移动高光；详情数字仍只显示确认值，断线重连保持当前阶段边界，`refreshAll` 成功后才显示 100%，并尊重 `prefers-reduced-motion`。
- 2026-09-23: 完成同步预测进度实现：新增显式同步生命周期与单调预测 Hook，乱序阶段事件不会倒退，刷新成功后才收束到 100%；Storybook 与 Dashboard E2E 覆盖默认收起、主动恢复、移动端、断线/失败和 reduced-motion 契约。
- 2026-09-23: 收敛剩余入口：移除 Inbox 卡片与命令面板中的局部同步动作，空态统一引导顶部同步；补充生命周期终态、reduced-motion、暂时断线与乱序事件回归证据。
- 2026-09-23: 修复同阶段重复 SSE 覆盖确认工作量的问题：已确认阶段只接受严格前进事件，重放或倒序事件不再改写详情或触发页面刷新。
